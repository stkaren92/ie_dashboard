\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{eso-pic}
\usepackage{sectsty}
\usepackage{fontspec}
\usepackage{titlesec}
\pagestyle{empty}
\usepackage{xcolor}
\usepackage{fontspec}

\setmainfont{Cronos Pro}[
      Path = font/,
      Extension = .otf,
      UprightFont = *-Regular
    ]

%% Set page size with a wider right margin
\geometry{a4paper, left=30mm, top=20mm, bottom=30mm, right=15mm}

%% Let's define some colours
\definecolor{cosmos}{HTML}{03697F}

% ===== Left border touching the page edge =====
\AddToShipoutPicture{
  \AtPageLowerLeft{
    % No space: bar begins exactly at x = 0
    \put(0,0){
      \color{cosmos}\rule{2cm}{\LenToUnit\paperheight}
    }
  }
}

% ===== Logo in bottom-right =====
\AddToShipoutPictureBG{
  \AtPageLowerLeft{
    % Bottom-right placement:
    % x = paperwidth - margin - logo_width
    % y = bottom margin
    \put(\LenToUnit{\paperwidth-2.5cm},0.7cm){
      \includegraphics[width=2cm]{img/cosmos_logo.png}
    }
  }
}